How to identify if Two pointer is applicable?
- subarray (longest/shortest/count) with some criteria

If some criteria is true for a subarray with L, R as starting point
then if there is another subarray with L', R' then,

Then, if L' > L then, R' >= R then, we can apply two pointers.